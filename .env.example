# Inkspire - Local Development Environment Variables
# Copy this file to .env.local and fill in your values
# The .env.local file is gitignored and won't be committed
#
# Usage:
#   cp .env.example .env.local
#   # Edit .env.local with your values
#   docker-compose up -d

# =============================================================================
# OpenAI Configuration (for AI consistency checking)
# =============================================================================

# OpenAI API Key - Get yours from: https://platform.openai.com/api-keys
# Leave empty to disable AI consistency checking (the app will still work)
# Note: Use double underscores (__) for .NET configuration hierarchy
OpenAI__ApiKey=sk-your-api-key-here
